services:
  mysql:
    image: anonymous-user/mysql:latest
    container_name: mysql-server
    restart: unless-stopped
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
